{
  "name": "takarakuzi-life",
  "version": "1.0.0",
  "description": "宝くじ生活 — 当選金だけで暮らす放置ゲーム",
  "private": true,
  "scripts": {
    "build": "node scripts/apply-store-config.js && node scripts/sync-www.js",
    "sync": "npm run build && npx cap sync",
    "check:store": "node scripts/check-store.js",
    "package:store": "node scripts/check-store.js && npm run sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "preview": "npx --yes serve www -p 5173"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/app": "^7.1.0",
    "@capacitor/browser": "^7.0.2",
    "@capacitor/core": "^7.4.3",
    "@capacitor/haptics": "^7.0.2",
    "@capacitor/ios": "^7.4.3",
    "@capacitor/status-bar": "^7.0.3",
    "@capacitor-community/admob": "^7.2.0",
    "@capgo/native-purchases": "^7.14.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
